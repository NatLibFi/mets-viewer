METS-viewer



Dependencies:

yum install perl-XML-LibXML.x86_64
yum install ImageMagick-perl.x86_64
yum install perl-XML-Simple.noarch


imagemagick will use /tmp for default temp dir, if that's not suitable, use:
export MAGICK_TEMPDIR=/data/tmp



License is in the LICENCE file (MIT)

